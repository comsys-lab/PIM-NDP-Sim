# GQA Scoring per (decoder layer * batch)
# - Qwen3MoE Configuration
#   - S_len = 1024
#   - #Query heads = 32 (Qwen3MoE)
#   - #KV heads = 4 (Qwen3MoE)
# - Input operand size
#   - Query: H_q * (1 * d_k) = 8 KiB
#   - Key Cache: (H_q / H_kv) * H_kv * (S_len * d_k) = 4 MiB
#   - Value Cache: H_kv * (H_q / H_kv) * (S_len * d_k) = 4 MiB
# - Output operand size: H_q * 1 * S_len = 64 KiB

# 1. Transpose the key
# - 
# - 

# 2. Project the grouped queries to the key
# - GEMV: 1*d_k vector and d_k*S_len matrix
# - (H_q) GEMV operations in parallel
MAC_ABK 0xffff_ffff_
MAC_ABK 0xffff_ffff_
MAC_ABK 0Xffff_ffff_

# 3. Multiply a scalar (sqrt(d)) and the projection results.
# - 
EWMUL
EWMUL

# 4-1. Row-wise softmax: find an exponent matrix of the scaled projection results.
# - 

# 4-2. Row-wise softmax: find the sum of exponents across the row dimension.
# - 

# 4-3. Row-wise softmax: divide the exponent by its sum.
# - 

# 5. GQA Output (weighted sum) 
